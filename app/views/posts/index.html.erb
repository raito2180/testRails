<% if @posts.present? %>
<% @posts.each do |post|%>
  <span>投稿名:</span><%= post.name %><br>
  <span>投稿内容:</span><%= post.text %><br>
<% end %>
<% else %>
  <span>投稿がありません</span>
<% end %>
<button>投稿を作成する</button>